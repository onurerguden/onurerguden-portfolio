import Link from "next/link";
import { notFound } from "next/navigation";
import { getProjects, isLocale } from "@/lib/content";
import { pageMetadata } from "@/lib/site";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))return {};return pageMetadata(locale,"/projects",locale === "en" ? "Projects" : "Projeler",locale === "en" ? "AI products, machine learning experiments and software engineering projects." : "AI ürünleri, makine öğrenmesi deneyleri ve yazılım mühendisliği projeleri.");}
export default async function ProjectsPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const en=locale === "en";const projects=getProjects(locale);return <main id="main"><div className="page-intro"><p className="section-kicker">{en ? "The project archive" : "Proje arşivi"}</p><h1>{en ? "Built. Tested. Learned." : "Geliştirdim. Denedim. Öğrendim."}</h1><p>{en ? "A collection of product work, research and engineering explorations. Each project starts with a different question." : "Ürünler, araştırmalar ve mühendislik çalışmaları. Her proje farklı bir soruyla başlıyor."}</p></div><div className="archive-grid">{projects.map(p=><article className="archive-entry" key={p.slug}><p className="project-category">{p.category}</p><h2>{p.featured ? <Link href={`/${locale}/projects/${p.slug}`}>{p.title} ↗</Link> : p.title}</h2><p>{p.summary}</p>{p.metric ? <p><strong>{p.metric.value}</strong> — {p.metric.label}</p> : null}<ul className="stack">{p.stack.map(s=><li key={s}>{s}</li>)}</ul>{!p.featured && p.repoUrl ? <p><a className="text-link" href={p.repoUrl}>{en ? "Source on GitHub" : "GitHub’da kaynak kod"} ↗</a></p> : null}</article>)}</div><section className="github-section"><div className="section-heading"><h2>{en ? "On GitHub" : "GitHub’da"}</h2><a className="text-link" href="https://github.com/onurerguden">{en ? "Visit profile" : "Profili ziyaret et"} ↗</a></div><p className="empty-note">{en ? "More experiments and source code live on my GitHub profile." : "Diğer deneyler ve kaynak kodlar GitHub profilimde."}</p></section></main>;}
